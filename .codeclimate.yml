engines:
  csslint:
    enabled: true
  phpmd:
    enabled: true
    config:
      file_extensions: "php"
      rulesets: "codesize,controversial,design,unusedcode"
  phpcodesniffer:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
      - javascript
      - php

ratings:
  paths:
    - src/**
    - "**.php"

exclude_paths:
  - vendor/**/*
  - .git/**/*
  - composer.*
